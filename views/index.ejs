<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mejia's Final - Import EV Application</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
<body>
<%- include("_header") -%>

    <h1>Mejia's Final Exam - Import EVs</h1>

    <% if(message === "success") { %>
        <h2>There are <b><%= vehicleCount %></b> vehicles in the database</h2>
    <% } else if (message) { %>
        <div class="alert <%= message.startsWith('Error') ? 'alert-danger' : 'alert-warning' %>" role="alert">
            <%= message %>
        </div>
    <% } %>

    <form action="/input" method="post" enctype="multipart/form-data">
        <div class="mb-3">
            <label for="vehicleFile" class="form-label">Select a file to import:</label>
            <input type="file" class="form-control" id="vehicleFile" name="vehicleFile" accept=".txt" required>
        </div>
        <button type="submit" class="btn btn-primary">Import Vehicles</button>
    </form>

    <% if (errors && errors.length > 0) { %>
        <h2 class="mt-3">Import Errors:</h2>
        <ul class="list-group">
            <% errors.forEach(errorItem => { %>
                <li class="list-group-item list-group-item-danger">
                    <strong>Line:</strong> <%= errorItem.line %><br>
                    <strong>Error:</strong> <%= errorItem.error %>
                </li>
            <% }); %>
        </ul>
    <% } %>

<%- include("_footer") -%>
</body>

</body>
</html>